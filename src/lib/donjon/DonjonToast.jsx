/* DonjonToast — donjon-fall-ui varianta Toast.
   Vizuálně identická s TkajUI (oktagonální shell a herní varianty jsou součástí TkajUI baze).
   Re-exportuje TkajUI implementaci (ToastProvider + useToast hook). */
export { ToastProvider, useToast } from '../tkajui/Toast'
